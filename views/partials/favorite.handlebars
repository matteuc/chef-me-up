<li class="favorite-block list-group-item" data-id="{{id}}" data-name="{{name}}">
    <div class="d-flex justify-content-between align-items-center">
        <span class="font-weight-bold text-muted">{{name}}</span>
        <div>
            <i data-id="{{id}}" class="favorite-btn text-danger fas fa-heart"></i>
            <i data-link="/recipes/{{id}}" class=" ml-2 text-info fas fa-info-circle"></i>
        </div>
    </div>
</li>